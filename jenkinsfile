pipeline {
  agent any
  stages {
    stage('Run Shell Script') {
      steps {
        sh 'chmod +x hello.sh'      // make script executable
        sh './hello.sh'             // run the script
        archiveArtifacts artifacts: 'run_output.txt', onlyIfSuccessful: true
      }
    }
  }
}

